import { styled } from "styled-components";

const Initiation = styled.span`
  font-weight: 700;
  font-size: 12px;
  text-transform: uppercase;
  color: ${({ theme }) => theme.fontSecondary};
`;

export { Initiation };
